@using CamelontaEpi.Models.ViewModels 
@using CamelontaEpi.Models.Pages 
@model  PageViewModel <BlogLandingPage> 
    @{
Layout = "~/Views/Shared/Layouts/_LeftNavigation.cshtml"; } 
<h1 @Html.EditAttributes(m => m.CurrentPage.MetaTitle)>
    @(Model.CurrentPage.MetaTitle ?? Model.CurrentPage.Name)
</h1>
<p class="introduction">
    @Html.PropertyFor(m => m.CurrentPage.MetaDescription)
</p>
<div class="row">
    <div class="span8">
        @Html.PropertyFor(m => m.CurrentPage.MainBody) @Html.PropertyFor(m => m.CurrentPage.BlogPostListing)
    </div>
</div>